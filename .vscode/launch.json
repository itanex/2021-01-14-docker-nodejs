{
    "configurations": [
        {
            "name": "Docker Node.js Launch",
            "type": "docker",
            "request": "launch",
            "preLaunchTask": "docker-run: debug",
            "platform": "node",
            "node": {
                "package": "${workspaceFolder}/docker-nodejs/package.json",
                "localRoot": "${workspaceFolder}/docker-nodejs"
            }
        }
    ]
}